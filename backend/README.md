## 当前已完成
* get_price_data(start_ts, end_ts, interval=15) 获取 CEX 和 DEX 的价格数据
* get_spread_data(start_ts, end_ts, interval=15)    获取价差数据
* run_backtest(start_ts, end_ts, z_threshold, trade_size_usdt)     执行回测
## 待完成
* get_correlation_data(start_ts, end_ts) 返回相关性数据
* get_heatmap_data(start_ts, end_ts) 返回热力图数据
  
## 注
* 所有时间（start_ts, end_ts）为时间戳（timestamp）
* 已实现函数部分返回值可能存在问题（逻辑存在错误，暂时不清楚具体实现思路），但会正常输出不会出现编译错误
* main函数仅用于测试函数是否正常运行，最终项目中可删除


# 2025/12/22更新：
* get_correlation_data(start_ts, end_ts) 返回相关性数据
    根据上次其他组同学的相关展示，会返回uniswap交易量与binance交易量的相关系数，不会返回对应的数据对
* get_heatmap_data(start_ts, end_ts) 返回热力图数据
    返回数据对结构：{小时起始时间戳，binance价格挡位，平均z_score}
    start_ms会自动改为整点，如 2025/09/30/12:30:30 的时间戳会修改为2025/09/30/12:00:00的时间戳
    小时起始时间戳：数据对的数据为小时起始时间戳之后一小时的数据统计，binance价格挡位同理，默认挡位水平是50（4300 表示 4300~4350）
    不存在的数据不会用返回值，比如2025/09/30/12:30:30的4300挡位如果没有数据，返回的数据对中不会有[2025/09/30/12:30:30对应时间戳, 4300, 0]的数据
* 套利机会分析算法优化
    加入了对滑点的考虑，会考虑交易量的大小对交易价格波动的影响

    
```
在非原子套利交易中，滑点的影响确实不容小觑，它可能直接吃掉你的利润甚至导致亏损。我来帮你梳理一下关键点：

滑点对非原子套利交易的核心影响
‌交易成本显著增加‌
滑点会导致实际成交价偏离预期，比如买入价更高或卖出价更低，这会直接压缩你的套利空间，甚至让原本盈利的交易变成亏损。

‌策略执行效果大打折扣‌
非原子套利依赖价格差快速获利，但滑点可能让你错过最佳成交时机，导致策略失效。比如，你计划在价差最大时平仓，但滑点可能让你以更差的价格成交。

‌心理压力与决策干扰‌
频繁的滑点会让你对策略产生怀疑，甚至做出非理性的交易决策，比如提前止损或过度交易。
```
    