# 会议纪要 — 2025-12-02

- **会议时间**：2025-12-02
- **会议记录**：魏文俐
- **组长**：魏文俐
- **队名**：ArbitraTech
- **参会人员**：方李庭、胡欣宇、施志煊

## 会议主题
- 前后端接口联调与数据格式确认
- 后端代码库清理与重构
- 数据量优化与算法集成进度

## 会议背景
临近中期展示（明日），团队需解决前端因数据量过大导致的性能卡顿问题，并完成前后端代码的最终整合与清理，确保演示版本的稳定性与代码仓库的整洁。

## 主要决议与讨论内容

### 1. 数据处理与性能优化

- **问题现状**：
  - 当前使用的 `merged_trading_data.csv` 虽为真实数据（9月1日），但数据量过大，导致前端页面加载卡顿，甚至未响应。
  - 现有的 CSV 格式不适合直接用于前端大规模展示。

- **解决方案**：
  - **前端策略**：方李庭已将清洗后的数据转换为 JSON 格式，用于首页流畅展示。为确保展示效果，展示时将优先使用前端本地写死的数据或 JSON 数据，避免实时请求大文件导致崩溃。
  - **后端任务**：胡欣宇与施志煊需进一步完善数学模型，对数据进行轻量化处理，以解决全量数据过大的问题。

### 2. 后端代码重构与清理

为确保提交的代码库清晰规范，团队对后端文件结构进行了整理：

- **核心文件保留**：
  - `api.py`：后端接口入口。
  - `service.py`：业务逻辑实现（含套利算法）。
  - `config.py`：配置文件。
  - 数据文件：`merged_trading_data.csv` 和 `arbitrage_signals.csv`。

- **清理冗余**：
  - 施志煊已将 `data_merger.py`、`init.sql`、`test_db.py` 等非核心或临时文件整理至 `base` 文件夹中。
  - `dbMapper` 进行了相应修改以适配新结构。
  - 删除了 `service.py` 中用于测试的 main 函数代码。

### 3. 算法与功能进度

- **套利算法**：
  - 目前前端已集成施志煊提供的基础套利算法。
  - 复杂的回测分析、热力图（三维数据：小时/区间/Z-score）及相关性分析（二维数据：滞后时间/相关系数）暂缓展示，优先保证基础功能的完整性。

- **后续计划**：
  - 中期展示后，后端需继续推进可视化支持、套利回测算法的深化及数据的进一步加工。
